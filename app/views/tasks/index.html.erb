<h1>タスク一覧</h1>

<ul>
  
<% @tasks.each do |tasks| %>

    <li>
<%= link_to tasks.id, tasks %>
    :  
<%= tasks.content %>
 >
<%= tasks.status %>
</li>
  
<% end %>

</ul>

<%= link_to '新規タスクの投稿', new_task_path %>